@model IEnumerable<Cascd_Dpdwn.Models.tbl_Territory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tr_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Region)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tr_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Region)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ter_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ter_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ter_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
